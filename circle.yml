machine:
  services:
    - docker

dependencies:
  override:
    - ci/circle_ci/build_image.sh

  cache_directories:
    - "~/docker-cache"

test:
  override:
    - docker run -it kibanaci/runner:$CIRCLE_SHA1:
        parallel: true
        files:
          - ci/tasks/*
