machine:
  services:
    - docker

dependencies:
  override:
    - ci/circle_ci/build_image.sh

  cache_directories:
    - "~/docker-cache"

test:
  override:
    - docker run -it kibanaci/runner:latest:
        parallel: true
        files:
          - ci/tasks/*
